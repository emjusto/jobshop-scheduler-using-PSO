import { dashboard } from './dashboard';
import { utilities } from './utilities';
import { other } from './other';
import { psoPages } from './pso-pages';

//-----------------------|| MENU ITEMS ||-----------------------//

const menuItems = {
    // items: [dashboard, psoPages, utilities, other]
    items: [dashboard, psoPages]
};

export default menuItems;
